pipeline {
    agent {
        label 'linux'
    }

    stages {
        stage('Ejecutar Python Script') {
            steps {
                sh "python hola-mundo.py"
            }
        }
    }
}
